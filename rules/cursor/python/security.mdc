---
description: Python 보안(시크릿 관리, 정적 분석)
globs: ["**/*.py", "**/*.pyi"]
alwaysApply: false
---
# Python Security

> 참고: Cursor 규칙은 다른 문서를 “자동 상속/병합”하지 않습니다. 공통 보안 규칙이 필요하면 `rules/common/security.md`를 별도 Cursor 규칙으로 옮기거나, 필요한 내용을 이 문서에 통합하세요.

## Secret Management

```python
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.environ["OPENAI_API_KEY"]  # Raises KeyError if missing
```

## Security Scanning

- Use **bandit** for static security analysis:
  ```bash
  bandit -r <your_package_or_src_dir>/
  ```

## Reference

See skill: `django-security` for Django-specific security guidelines (if applicable).
